[package]
name = "balancer"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.10.1"
umbral-socket = "0.2.5"
crossbeam-channel = "0.5.15"
mio = { version = "1.0.4", features = ["net", "os-poll"] }
